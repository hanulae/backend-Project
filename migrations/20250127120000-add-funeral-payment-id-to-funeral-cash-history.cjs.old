'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.addColumn('funeral_cash_histories', 'funeral_payment_id', {
      type: Sequelize.UUID,
      allowNull: true,
      references: {
        model: 'funeral_payments',
        key: 'funeral_payment_id',
      },
      onUpdate: 'CASCADE',
      onDelete: 'SET NULL',
    });
  },

  async down(queryInterface) {
    await queryInterface.removeColumn('funeral_cash_histories', 'funeral_payment_id');
  },
};
